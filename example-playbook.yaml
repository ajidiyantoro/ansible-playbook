- name: Install Nginx
  hosts: all  

  tasks:
    - name: Install Nginx package
      apt:
        name: nginx
        state: present
      become: yes   

    - name: Start Nginx service and enable it at boot
      service:
        name: nginx
        state: started
        enabled: yes